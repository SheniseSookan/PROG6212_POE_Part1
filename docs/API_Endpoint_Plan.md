| HTTP Method | Route | Description | Role Required | Request Body | Expected Response |
| :--- | :--- | :--- | :--- | :--- | :--- |
| `POST` | `/api/auth/register` | Registers a new user account as either an Organiser or Participant. | None (public) | `{ "fullName": "string", "email": "string", "password": "string", "role": "string" }` | **201 Created**: Account created successfully.<br>**400 Bad Request**: Invalid data or email already exists. |
| `POST` | `/api/auth/login` | Authenticates a user and generates a JWT bearer token for session access. | None (public) | `{ "email": "string", "password": "string" }` | **200 OK**: `{ "token": "jwt_string", "role": "string" }`.<br>**401 Unauthorized**: Invalid credentials. |
| `GET` | `/api/users/profile` | Retrieves the profile details of the currently logged-in user. | Any (logged in) | None | **200 OK**: User profile JSON.<br>**401 Unauthorized**: Missing or invalid token. |
| `PUT` | `/api/users/profile` | Updates the contact details or name of the logged-in user. | Any (logged in) | `{ "fullName": "string", "contactNumber": "string" }` | **200 OK**: Profile updated successfully.<br>**400 Bad Request**: Validation error. |
| `GET` | `/api/events` | Retrieves a list of all upcoming events (used by Participants to browse). | None (public) | None | **200 OK**: Array of event objects. |
| `GET` | `/api/events/{id}` | Retrieves full details of a specific event, including its nested categories. | None (public) | None | **200 OK**: Event object details.<br>**404 Not Found**: Event ID does not exist. |
| `POST` | `/api/events` | Creates a new event record linked to the Organiser. | Organiser | `{ "title": "string", "description": "string", "location": "string", "startDate": "datetime", "endDate": "datetime" }` | **201 Created**: Event created successfully.<br>**400 Bad Request**: Invalid date logic.<br>**403 Forbidden**: Wrong role. |
| `PUT` | `/api/events/{id}` | Modifies an existing event's details. | Organiser | `{ "title": "string", "description": "string", "location": "string", "startDate": "datetime", "endDate": "datetime" }` | **200 OK**: Event updated.<br>**404 Not Found**: Event not found.<br>**403 Forbidden**: Not the event owner. |
| `DELETE` | `/api/events/{id}` | Deletes an event and all its associated categories. | Organiser | None | **204 No Content**: Deletion successful.<br>**404 Not Found**: Event not found. |
| `GET` | `/api/events/{eventId}/categories` | Lists all distance/race categories for a specific event. | None (public) | None | **200 OK**: Array of category objects. |
| `POST` | `/api/events/{eventId}/categories` | Adds a new race category (e.g., 10km Run) to an existing event. | Organiser | `{ "categoryName": "string", "distanceKm": float, "entryFee": float, "maxParticipants": int }` | **201 Created**: Category added successfully.<br>**404 Not Found**: Parent event not found. |
| `POST` | `/api/enrolments` | Enters the logged-in Participant into a specific race category. | Participant | `{ "categoryId": int }` | **201 Created**: Enrolment successful.<br>**409 Conflict**: Already enrolled or capacity reached. |
| `GET` | `/api/enrolments/my-enrolments` | Retrieves all enrolments specific to the logged-in Participant. | Participant | None | **200 OK**: Array of user's enrolments.<br>**401 Unauthorized**: Missing token. |
| `GET` | `/api/events/{eventId}/enrolments` | Retrieves a list of all participants enrolled in a specific event. | Organiser | None | **200 OK**: Array of enrolment records.<br>**403 Forbidden**: Not the event owner. |
| `POST` | `/api/results` | Captures a participant's race result (time, position, status). | Organiser | `{ "enrolmentId": int, "finishTime": "time", "overallPosition": int, "categoryPosition": int, "status": "string" }` | **201 Created**: Result captured.<br>**409 Conflict**: Result already exists for this enrolment. |
| `GET` | `/api/events/{eventId}/results` | Fetches the full results/leaderboard for a specific event. | None (public) | None | **200 OK**: Array of results ordered by finish time/position. |
| `GET` | `/api/results/my-results` | Retrieves the historical personal performance records of the logged-in Participant. | Participant | None | **200 OK**: Array of the participant's past results. |